Use latest utils available in clojure and clojure-contrib
Remove dependencies on local environment
Nice README
Make useful for public consumption
Make it easier to generate docs for custom set of libs
Document generator interface
See if we can trim utils
Fix licensing text - needs to be customizable
Perhaps separate namespaces for utils